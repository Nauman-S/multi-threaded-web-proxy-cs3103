procedure findPrimes {
    read x;                       // 1
    i = 2;                        // 2
    while (i < x) {               // 3
        flag = 1;                 // 4
        call checkPrime;          // 5
        if (flag == 1) {          // 6
            call printPrime;      // 7
        }
        i = i + 1;                // 8
    }
}

procedure checkPrime {
    j = 2;                        // 9
    while (j < i) {               // 10
        if(i % j == 0) {          // 11
            flag = 0;             // 12
        }
    }
}

procedure printPrime {
    print i;                      // 13
}

procedure x {
    
}