procedure test {
    read x;
    read y;
    while ((a > 0) || ((b > 0) && (d + c == 3))) {
        if ((x + y) > (3 * y)) then {
            print x;
        } else {
            e = (procedure + test) * (read - test) + 3;
        }
    }
}
