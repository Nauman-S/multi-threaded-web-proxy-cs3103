1 - select BOOLEAN without clause
while w, w2; assign a;
Select BOOLEAN
TRUE
5000
2 - select wrong BOOLEAN without clause
while w, w2; if ifs;
Select boolean
SemanticError
5000
3 - select BOOLEAN expecting false result
while w, w2; assign a;
Select BOOLEAN such that Follows(2, 4)
FALSE
5000
4 - select BOOLEAN when BOOLEAN is declared as synonym
while w, BOOLEAN;
Select BOOLEAN
17, 7
5000
5 - select BOOLEAN using Follows
assign a1, a2;
Select BOOLEAN such that Follows(a1, a2)
TRUE
5000
6 - select BOOLEAN using Parent
if ifs; while w;
Select BOOLEAN such that Parent(ifs, w)
TRUE
5000
7 - select BOOLEAN var refers to different objects
call c; assign a;
Select BOOLEAN such that Follows(c, a) and Follows(a, c)
FALSE
5000
8 - select BOOLEAN using two Next
assign a1, a2; if ifs;
Select BOOLEAN such that Next(ifs, a1) and Next(ifs, a2)
TRUE
5000
9 - select BOOLEAN using Parent*
print p; while w;
Select BOOLEAN such that Parent*(w, p)
FALSE
5000
10 - select BOOLEAN using Modifies and with
read r; variable v;
Select BOOLEAN such that Modifies(r, v) with v.varName = "a"
TRUE
5000
11 - select BOOLEAN using Uses and with
print p; variable v;
Select BOOLEAN such that Uses(p, v) with v.varName = "z"
TRUE
5000
12 - select BOOLEAN using Follows*

Select BOOLEAN such that Follows*(8, 13)
TRUE
5000
13 - select BOOLEAN using Next*
call c1, c2; read r;
Select BOOLEAN such that Next*(r, c2) and Next*(c2, c1)
TRUE
5000
14 - select BOOLEAN using Calls
procedure p1, p2, p3;
Select BOOLEAN such that Calls(p1, p2) and Calls(p2, p3)
TRUE
5000
15 - select BOOLEAN using Calls*
procedure p1, p2;
Select BOOLEAN such that Calls*(p1, p2) and Calls*(p2, p1)
FALSE
5000
16 - select BOOLEAN using Calls, Modifies and with
procedure p1, p2;
Select BOOLEAN such that Calls(p1, p2) and Modifies(p2, "z") with p1.procName="Example"
TRUE
5000
17 - select BOOLEAN using Next, Parent and with
while w; assign a;
Select BOOLEAN such that Next(w, a) and Parent(w, a) with w.stmt# = 7
TRUE
5000
18 - select BOOLEAN using Uses, Modifies and with
assign a; variable v;
Select BOOLEAN such that Uses(a, v) and Modifies(a, v) with v.varName = "x"
TRUE
5000
19 - select BOOLEAN using Uses, Modifies and pattern wildcard
assign a; variable v;
Select BOOLEAN such that Uses(a, v) and Modifies(a, v) pattern a(_,_)
TRUE
5000
20 - select BOOLEAN using Uses, Modifies and pattern half wildcard
assign a; variable v;
Select BOOLEAN such that Uses(a, v) and Modifies(a, v) pattern a(_,_"z * 3"_)
FALSE
5000